# ===========================================
# Spotify LINE POC - Environment Variables
# ===========================================

# Database (PostgreSQL)
DATABASE_URL="postgresql://postgres:password@localhost:5432/spotify_line_poc?schema=public"

# App Base URL (本地開發用 http://localhost:3000)
APP_BASE_URL="http://localhost:3000"

# Session Secret (用於加密 session cookie，請產生隨機字串)
# 可用指令產生: openssl rand -base64 32
SESSION_SECRET="your-super-secret-session-key-at-least-32-chars"

# ===========================================
# LINE Login (OAuth 2.0)
# ===========================================
# 從 LINE Developers Console 取得:
# https://developers.line.biz/console/
# 1. 建立 Provider
# 2. 建立 LINE Login Channel
# 3. 在 LINE Login 設定中填入 Callback URL
LINE_CHANNEL_ID="your-line-channel-id"
LINE_CHANNEL_SECRET="your-line-channel-secret"
LINE_REDIRECT_URI="http://localhost:3000/api/auth/line/callback"

# ===========================================
# Spotify (OAuth 2.0)
# ===========================================
# 從 Spotify Developer Dashboard 取得:
# https://developer.spotify.com/dashboard
# 1. 建立 App
# 2. 在 Settings 中設定 Redirect URI
SPOTIFY_CLIENT_ID="your-spotify-client-id"
SPOTIFY_CLIENT_SECRET="your-spotify-client-secret"
SPOTIFY_REDIRECT_URI="http://localhost:3000/api/auth/spotify/callback"
